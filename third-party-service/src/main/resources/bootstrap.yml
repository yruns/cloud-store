

spring:
  application:
    name: third-party-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        namespace: dev
        group: cloud-store-service
        file-extension: yaml
        refresh-enabled: true

  profiles:
    active: dev # ???
